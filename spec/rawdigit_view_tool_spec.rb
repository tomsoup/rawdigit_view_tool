require 'spec_helper'

describe RawdigitViewTool do
  it 'has a version number' do
    expect(RawdigitViewTool::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
